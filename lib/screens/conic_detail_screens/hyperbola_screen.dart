import 'package:flutter/material.dart';

class HyperbolaScreen extends StatelessWidget {
  static const routeName = '/hyperbola-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hyperbola')),
    );
  }
}

import 'package:flutter/material.dart';

class PairOfLinesScreen extends StatelessWidget {
  static const routeName = '/pair-of-lines-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pair Of Lines')),
    );
  }
}

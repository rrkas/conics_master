import 'package:flutter/material.dart';

class ParabolaScreen extends StatelessWidget {
  static const routeName = '/parabola-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Parabola')),
    );
  }
}

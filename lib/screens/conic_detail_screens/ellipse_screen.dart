import 'package:flutter/material.dart';

class EllipseScreen extends StatelessWidget {
  static const routeName = '/ellipse-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ellipse')),
    );
  }
}
